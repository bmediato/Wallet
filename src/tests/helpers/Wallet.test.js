// import React from 'react';
// import { screen } from '@testing-library/react';
// import userEvent from '@testing-library/user-event';
// import App from '../../App';
// import { renderWithRouterAndRedux } from './renderWith';
// import Wallet from '../../pages/Wallet';

describe('Testa página Wallet', () => {
  test('Testa se a aplicação contém o  da pessoa logada', () => {

  });
  test('testa se a aplicação tem a quantidade de itens adicionados na tela', () => {

  });
  test('testa se a aplicação contém o texto BRL redenrizado', () => {

  });
  test('testa se a aplicação contém um input com o valor da despesa', () => {

  });
  test('testa se a aplicação contém um input para descrição', () => {

  });
});
